apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: nginx
  labels:
   env: test  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
      env: test 
  template:
    metadata:
      labels:
        app: nginx
        env: test 
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
